/* eslint-disable no-unused-vars */
type AvatarElement = {
  NFTName: string;
  Description: string;
  AvatarAsset: string;
  ElementName: string;
};

type Avatar = {
  AvatarElement: AvatarElement;
};
