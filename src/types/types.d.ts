/* eslint-disable no-unused-vars */
type Tab = {
  name: string;
  link: string;
};

type Transfer = {
  BlockNumber: string;
  Quantity: string;
  From: string;
  To: string;
  Timestamp: string;
};
